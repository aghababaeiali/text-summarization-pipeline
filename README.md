End-to-end text summarization system with:

- FastAPI backend
- Transformer-based summarization model (FLAN-T5-Small, LoRA fine-tuned on SAMSum)
- Simple HTML UI
- Docker containerization
- Deployment-ready for Render